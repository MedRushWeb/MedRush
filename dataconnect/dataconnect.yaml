specVersion: "v1"
serviceId: "final-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "final-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
